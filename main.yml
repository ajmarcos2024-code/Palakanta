- name: Checkout code
  uses: actions/checkout@v4

- name: Fix Permissions
  run: |
    if [ -f "android/gradlew" ]; then
      chmod +x android/gradlew
    elif [ -f "gradlew" ]; then
      chmod +x gradlew
    else
      echo "Gradlew not found! Listing files:"
      ls -R
      exit 1
    fi
